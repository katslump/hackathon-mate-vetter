import React from 'react';

class Idea extends React.Component {
    render() {
        return (<div style={{display: "inline-flex"}}>
            this is an idea
        </div>);
    }
};

export default Idea;
